#include <bits/stdc++.h>
using namespace std;

struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
};

int traversal(TreeNode* root, int dir, int curr) {
    int ans = 0, left, right;
    if (root) {
        
       
    }
    return ans;
}


int longestZigZag(TreeNode* root) {
    return traversal(root, 0, 0);
}

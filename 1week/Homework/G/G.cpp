#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios ::sync_with_stdio(0);
    cin.tie(0);
    int num_str;
    string input;
    cin >> num_str;
    int largest_size = 0;
    vector<string>table;
    for (int i  = 0; i < num_str; i++){
        cin >> input;
        if (input.size() > largest_size){
            largest_size = input.size();
        }
        table.push_back(input);

    }
    for (int i = 0; i < largest_size; i++){
        for ()
    }


}
#include <bits/stdc++.h>
using namespace std;

long long n, l, r, input, num_inputs;

int main()
{
    ios ::sync_with_stdio(0);
    cin.tie(0);
    
    cin >> n >> l >> r;
    cin >> input;
    cin >> num_inputs;
    
}
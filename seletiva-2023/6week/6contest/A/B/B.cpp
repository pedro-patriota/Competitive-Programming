#include <bits/stdc++.h>
using namespace std;
#define ll long long
ll test_cases, length;

int main()
{
    ios ::sync_with_stdio(0);
    cin.tie(0);
    cin >> test_cases;
    for (ll i = 0; i < test_cases; i++){
        cin >> length;
        vector<ll> arr(length);
        for (ll j = 0; j < length; j++){
            cin >> arr[j];
        }
        
    }
    
    
}